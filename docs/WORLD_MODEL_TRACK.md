# World Model Track

Status: `SCAFFOLDED`.

Initial implementation order:

1. deterministic Tier 0 environments;
2. tiny NumPy action-conditioned JEPA;
3. no-action and shuffled-action controls;
4. named activations;
5. interventions and matched controls;
6. planning smoke loop;
7. workspace criteria with null-result-safe controls.

Published world-model adapters are `BLOCKED_RESOURCE` on the CPU VPS.
