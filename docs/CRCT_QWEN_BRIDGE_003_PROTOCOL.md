# CRCT-QWEN-BRIDGE-003 protocol

Status: `PREREGISTERED_NOT_RUN`.
`execution_authorized: false`.

Bridge from coalition-aware CRCT ontology (`CRCT-COALITION-IBD-001` evaluator,
with the tautological-gauge caveat recorded in RESULTS) onto Qwen binding
**only after** `LLM-QWEN-BINDING-ALGEBRA-004` B0 passes.

This bridge must not open V3 `test`/`paraphrase`. It must not relabel HARD-002
or V3.

## What is frozen now

- HARD-002 = `NEGATIVE_RESULT` on seeds 1009/2027/4093. Never reuse those seeds.
- V3 = `INELIGIBLE_TASK_PHASE0`.
- Confirmation = competence only, evidence level Availability.
- Required CRCT objects if B1 is later authorized: literal recall vs
  epsilon sufficiency, minimal sets, necessity, redundancy, signed
  cancellation, equivalent circuits, intervention-support labels, IID/OOD,
  and a **non-tautological** gauge (compensated reparameterization actually
  applied to parameters).
- Fair Intervention-JEPA baselines including capacity-matched direct-delta.

## What is not authorized

- Any Qwen forward.
- Treating m-kNN, stitching, or LeFlow as Qwen circuit evidence.
- Workspace claims.

## Successor evaluator

`CRCT-COALITION-IBD-002` is the prospective plant/evaluator version with a
real applied gauge. Its confirmation seeds are not the IBD-001 confirmation
seeds. See `docs/CRCT_COALITION_IBD_002_PROTOCOL.md`.
