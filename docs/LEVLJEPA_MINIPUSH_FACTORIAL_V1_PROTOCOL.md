# LeVLJEPA MiniPush factorial V1

Experiment: `WM-LEVLJEPA-MINIPUSH-FACTORIAL-001`

Status: `PREREGISTERED_NOT_RUN`. Secondary track. No download is authorized
by this protocol. Not on the Qwen/CRCT critical path.

Config: `configs/experiments/levljepa_minipush_factorial_v1.yaml`.

Compare the current MiniPush encoder against a frozen LeVLJEPA encoder, with
and without SIGReg only if the adapter can apply it without changing the
action-conditioned predictor or the data budget. Metrics: effective rank,
object localization probes, causal patching specificity, planning/closed-loop.

A better dense representation is not a workspace and is not a circuit.
