# Independent protocol review — CRCT-LEARNED-WM-CONDITIONAL-MEDIATOR-006

Pre-freeze. No 006 training. 001–005 not mutated.

Independent [protocol review](32c96334-8148-478c-8088-3faa3772fcb2): first
candidate **NO-FREEZE** (F2 descendant recompute dropped `V_up` `b2_*`
patches). Repair: `_recompute_r2` reapplies P-forward `b2_*` hid2
overrides; plant `V_up=["b2_0"]` locked.

Second **NO-FREEZE**: protocol first-match order lagged the repaired
`adjudicate_seed` (gauge mismatch after redundant/interacting). Repair:
protocol order now matches code.

Independent [protocol re-review](32c96334-8148-478c-8088-3faa3772fcb2):
**FREEZE_ALLOWED**. P0 none.

Level 3 is not authorized. Action-stem `V_up` may be Level 2B. Cached
`r2_P` does not assign class. Mean-fill MSRS is not `G_V`. KEY TEST is
an executable plant hard gate.
