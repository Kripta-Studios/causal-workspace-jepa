# Approach Registry

| Family | Owner | Question | Evidence | Blockers | Next Experiment | Status |
| --- | --- | --- | --- | --- | --- | --- |
| Architecture/interfaces | root agent | Can the project run reproducibly in CPU mode? | Resource guard, typed interfaces, tests, and audits pass. | None for CPU smoke. | Keep audit green. | `SMOKE_VALIDATED` |
| Tier 0 dynamics | root agent | Can deterministic simulators expose ground truth? | Five deterministic generators and manifests. | Scientific scale. | Add OOD parameter splits. | `SMOKE_VALIDATED` |
| Tiny JEPA | root agent | Does action conditioning improve toy prediction? | Conditioned model beats no-action and shuffled controls. | Linear/random encoder limits mechanism claims. | Multi-consumer audit. | `SMOKE_VALIDATED` |
| Intervention primitives | root agent | Are interventions serializable and replayable? | Operators and replay tests pass. | Module-specific adapters remain. | Use direct replay in every causal experiment. | `SMOKE_VALIDATED` |
| Workspace discovery | root agent | Can a compact shared causal subspace be distinguished from private channels and generic state geometry? | `WM-T0-005` adds held-out task composition, task swaps, local-tangent controls, and three seeds; all seeds reject the candidate. | Current post-hoc consumers do not transfer compositionally; published-model and reportability tests remain unavailable. | Redesign the world model so goal/value/risk consumers are trained jointly, then preregister a new architecture rather than tuning WM-T0-005. | `ACTIVE` |
| Mock Qwen | root agent | Can known activation dynamics validate LLM pipeline? | Mock intervention-JEPA smoke passes. | Not evidence about a real LLM. | Add stronger baselines. | `SMOKE_VALIDATED` |
| GPT-2 Medium | root agent | Can intervention effects be predicted on a real transformer? | Coordinate and magnitude-6 contrast edits are locally additive; prompt-local Jacobians work, but learned/corpus transport fails across prompts. | One layer, one seed, selected outputs, and constructed direction labels. | Move to replacement/resampling or feature clamps on `gpu_12gb`; retain held-out prompts and local Jacobians. | `SMOKE_VALIDATED` |
| Published JEPA adapters | root agent | Can published world models support the same audit? | All three faithful small LeWorldModel seeds pass reproduction gates; planner intervention passes two seeds. | Donor-decoding specificity and full restricted circuit pass only one seed; aggregate graph rejected; other adapters remain placeholders. | Retain this negative result; a new method requires a new preregistration. | `SMOKE_VALIDATED` |
| Real Qwen hooks | root agent | Can HF Qwen interventions be predicted and verified? | Pinned Qwen3-0.6B hooks, five operations, autograd, bounded data, predictive baselines, and direct candidate execution pass their software gates. | Sites/prompts are narrow and no candidate circuit survived. | Broaden only after the world-model milestone. | `SMOKE_VALIDATED` |
| Qwen intervention data | root agent | Can direct Qwen effects be stored without leakage or unbounded capture? | 432 real effects in a checksum-verified HDF5 shard; split/donor/storage gates pass. | Small prompt/site grid. | Freeze data and evaluate meta-models/baselines. | `SMOKE_VALIDATED` |
| Qwen conditional meta-model | root agent | Can a compressed nonlinear predictor generalize beyond exact local transport? | Corrected FP32 audit passes numerically; exact JVP and quadratic Taylor beat all three legacy bottleneck seeds. | Model lacks a target encoder/JEPA objective; selected edits are mostly local; ranked graph failed. | Build a genuine target-encoder model on behavior-changing semantic data with exact/quadratic controls. | `COMPLETED_NEGATIVE` |
| Qwen semantic donor transfer | root agent | Can a meta-model predict finite edits that change the model's factual answer? | All dataset gates pass: 612 direct patches, 93.6% top-token change, 50% held-out donor transfer, and converged exact-JVP controls. A genuine target-encoder model and frozen comparison are implemented. | One prompt family and 0.6B model; learned study not yet executed. | Commit/push `LLM-TARGET-IJEPA-001`, then execute once without changing gates. | `IMPLEMENTED_UNVALIDATED` |
| Context-conditioned causal geometry | root agent | Is there a low-rank intervention-reachable and behavior-observable subspace beyond known balancing methods? | Literature/adversarial audit and an analytic cross-context pooling counterexample are recorded. | Generic Gramian/CoBRAS balancing is prior art; current Qwen data have almost no behavior changes and current LeWM planner is weak. | Require within-context modes, permutation/pooling nulls, coordinate-gauge tests, manifold-valid edits, and direct behavior fidelity. | `PREREGISTRATION_REQUIRED` |
| Qwen3-4B bounded capture | root agent | Can the primary dense target fit selected-site capture on 12 GB VRAM? | Exact pinned revision, 180 selected rows, and checksum/budget gates pass. | Availability only; no 4B interventions, gradients, or behavior. | Keep bounded or preregister a causal extension. | `SMOKE_VALIDATED` |
| Completion audit | root agent | Does the repository meet every explicit bounded completion criterion from clean pushed code? | All 14 criteria plus corrected JVP disposition, 68 tests, Ruff, and reproducibility checks pass. | Does not validate rejected hypotheses or optional cluster routes. | Keep green after later milestones. | `SMOKE_VALIDATED` |
| SkyJEPA | future external agent | Can long-horizon flight model be audited? | None. | Official assets unavailable/unverified. | Recheck official release. | `BLOCKED_EXTERNAL` |
